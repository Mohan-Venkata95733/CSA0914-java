// 11. Create a Set and find the following
// a) Find unique elements in a Set
// b) Find the largest element in a Set

import java.util.*;

class SetAnalysis {
    public static void main(String[] args) {
        Set<Integer> set = new HashSet<>(Arrays.asList(5, 2, 8, 1, 9, 3, 3, 5));

        // a) Find unique elements in a Set
        Set<Integer> uniqueElements = new HashSet<>(set);
        System.out.println("Unique elements: " + uniqueElements);

        // b) Find the largest element in a Set
        int largestElement = Collections.max(set);
        System.out.println("Largest element: " + largestElement);
    }
}
